# Baseline-Protokoll vom 30. Juli 2026

## Quellstand

- Basis: `origin/main`
- Commit: `48e8bdb` (`Merge pull request #17 from c4kingpin/fix/form-field-readability`)
- Ausbauzweig: `agent/wave-0-baseline`

Die zuvor offenen C07-Dateien wurden unverändert auf
`agent/c07-deployment-operations-v2` im lokalen Checkpoint-Commit `5ee1228`
gesichert. Sie sind nicht Teil dieses Ausbauzweigs.

## Anwendungsbaseline

`mix precommit` war erfolgreich:

- Kompilierung ohne Warnungsfehler
- Formatierung erfolgreich
- 108 Tests bestanden

ExUnit meldet weiterhin den bereits vorhandenen Hinweis, dass
`test/fixtures/private/synthetic-review/generate_synthetic_documents.exs`
nicht dem Test-Dateimuster entspricht. Die Datei ist ein Fixture-Generator und
kein ExUnit-Test.

## Offizielles Formular und PDF-Pipeline

Der reproduzierbare Versuch
`scripts/c00/verify_pdf_pipeline.sh` war erfolgreich. Er hat:

- das offizielle deutsche DB-Fahrgastrechteformular geladen
- die erwartete SHA-256-Prüfsumme
  `4a30f9c7f00593bf5bda1b6eaa2d1b6e293357faa48631a1d7e2ade3b77a39a9`
  bestätigt
- die Formularversion `Formular 2025 (ME/08/25)` und alle erwarteten
  AcroForm-Felder verwendet
- synthetische Werte sichtbar befüllt
- Formular, Ticket und Rechnung zu vier A4-Seiten zusammengeführt
- AcroForm, JavaScript, offene Aktionen und eingebettete Dateien aus dem
  Ergebnis entfernt
- Ticket- und Rechnungstext im erzeugten Gesamt-PDF nachgewiesen

Die Prüfung verwendet ausschließlich synthetische Fixtures und legt ohne
`C00_OUTPUT_DIR` keine Ausgabe dauerhaft ab.

## Bekannte Produktblocker

- In Produktion gibt es noch keinen Authentik-Login.
- Die Oberfläche bindet `Fahrgastrechte.Rail` und
  `Fahrgastrechte.Exports` noch nicht in die Antragsstrecke ein.
- Reiseergebnis, Fahrtrichtung, neutrale Anrede und lokale Bahnzeiten müssen
  vor einer produktiven PDF-Erzeugung formulargetreu korrigiert werden.
- C07 und C08 benötigen weiterhin eine praktische Betriebs- und
  Sicherheitsabnahme.
